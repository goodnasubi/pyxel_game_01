{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "install-uv",
            "type": "shell",
            "command": "command -v uv >/dev/null 2>&1 || pip install uv",
            "presentation": {
                "reveal": "never"
            }
        },
        {
            "label": "uv-run-debug-server",
            "type": "shell",
            "command": "uv run python -m debugpy --listen 5678 --wait-for-client ${file}",
            "isBackground": true,
            "problemMatcher": [],
            "dependsOn": ["install-uv"]
        }
    ]
}